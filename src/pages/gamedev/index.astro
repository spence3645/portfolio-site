---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProjectCard from '../../components/ui/ProjectCard.astro';
import { gamedevProjects } from '../../data/gamedev-projects';

const rawBase = import.meta.env.BASE_URL;
const base = rawBase.endsWith('/') ? rawBase : rawBase + '/';
const published = gamedevProjects
  .filter((p) => p.published)
  .sort((a, b) => a.order - b.order);
---

<BaseLayout title="Game Dev Projects — Matthew Spencer" description="A gallery of my game development work.">
  <div class="max-w-6xl mx-auto px-4 py-16">
    <div class="mb-12">
      <p class="text-xs font-mono uppercase tracking-widest text-accent-400 mb-2">Game Dev</p>
      <h1 class="text-4xl font-bold text-white mb-3">Featured Projects</h1>
      <p class="text-gray-400">Games, prototypes, and interactive experiments.</p>
    </div>

    {published.length === 0 ? (
      <p class="text-gray-500">No projects yet — check back soon.</p>
    ) : (
      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {published.map((project) => (
          <ProjectCard
            project={project}
            href={project.liveUrl}
          />
        ))}
      </div>
    )}
  </div>
</BaseLayout>
